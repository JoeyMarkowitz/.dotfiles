- clean: ['~']

- link:
    ~/.vim: vim/
    ~/.vimrc: vimrc
    ~/.bashrc: bashrc
    ~/.bash_profile: bash_profile

- shell:
  - [git submodule update --init --remote --recursive, Upgrading and Installing submodules]
  - [install_vim_pathogen.sh, Install vim pathogen]
  - [pip install dbgp vim-debug; install-vim-debug.py, Installing python vim debugger github.com/jaredly/vim-debug]
